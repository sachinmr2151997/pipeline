pipeline{
	agent { label 'master' }
	stages {
		stage('BUILD'){
			steps {
			sh 'echo this is my 1st pipeline job'
			sh 'ls -lrt'		
			}
		}
		stage('TEST'){
			steps {
			sh 'echo this is my 1st pipeline job'
			sh 'df -h'		
			}
		}
        }
}